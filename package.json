{
  "name": "mern-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:check": "tsc -p",
    "build:server": "tsc -p ./tsconfig.server.json",
    "build:client": "tsc -p ./tsconfig.client.json",
    "build": "npm:build:check && npm:build:server && npm:build:client",
    "start:server": "ts-node-dev -d src/server/server.ts",
    "start:client": "cd client && npm start",
    "start": "concurrently npm:start:server npm:start:client",
    "lint": "eslint . --ext .ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.0.14",
    "@typescript-eslint/eslint-plugin": "^3.5.0",
    "@typescript-eslint/parser": "^3.5.0",
    "eslint": "^7.3.1",
    "ts-node-dev": "^1.0.0-pre.50",
    "typescript": "^3.9.6"
  },
  "dependencies": {
    "concurrently": "^5.2.0"
  }
}
